# Local Development Server Configuration
# The backend server runs on localhost:3001 for API calls
API_SERVER=http://localhost:3001

# Matches are stored in: server/data/matches.json
# Images are stored in: server/uploads/upcoming-matches/

